#!/usr/bin/env node

"use strict";
var path = require('path');
var fs = require('fs');
var lib = path.join(path.dirname(fs.realpathSync(__filename)), '../lib');

var npc = require(lib+'/nativeplantcenter.js');
var foo = new npc();

foo.keyword("bittersweet", function(result) {
  console.log(JSON.stringify(result,null,"  "));
  
});

// heuchera
// bittersweet
